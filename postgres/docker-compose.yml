version: '3.8'
services:
  postgres:
    container_name: postgres_database
    restart: always
    environment:
      - POSTGRES_DB=music
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    build: './db'
    ports:
      - '5438:5432'